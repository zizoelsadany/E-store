# 🚀 خطوات رفع المشروع على GitHub

## الخطوة 1️⃣: إنشاء Repository على GitHub

1. اذهب إلى https://github.com/new
2. اكتب اسم المشروع مثلاً: `e-store-react` أو `ecommerce-app`
3. اختر **Public** أو **Private**
4. لا تختر "Initialize this repository with..." (لأننا بالفعل لدينا commits محلي)
5. اضغط **Create repository**

## الخطوة 2️⃣: ربط المشروع المحلي بـ GitHub

بعد إنشاء repository، سيظهر لك أوامر. اختر الأمر الثاني (Existing repository):

```bash
git remote add origin https://github.com/YOUR-USERNAME/e-store-react.git
git branch -M main
git push -u origin main
```

**ملاحظة مهمة:** استبدل:
- `YOUR-USERNAME` باسم حسابك على GitHub
- `e-store-react` باسم repository الذي أنشأته

## الخطوة 3️⃣: كيفية تنفيذ الأوامر

### إذا كنت على Windows PowerShell:

```powershell
cd "D:\project recat"

# أضف الـ remote
git remote add origin https://github.com/YOUR-USERNAME/e-store-react.git

# غيّر اسم الفرع إلى main
git branch -M main

# ادفع الملفات إلى GitHub
git push -u origin main
```

### إذا استخدمت Git Bash:

نفس الأوامر بالضبط لكن بدون الاقتباسات

## الخطوة 4️⃣: إدخال بيانات GitHub الخاصة بك

عند تشغيل `git push`، قد يطلب منك:
- **Username**: أدخل اسم حسابك على GitHub
- **Password/Token**: 
  - إذا فعّلت 2FA، استخدم GitHub token بدلاً من كلمة المرور
  - لإنشاء token: اذهب إلى Settings → Developer settings → Personal access tokens → Generate new token

## الخطوة 5️⃣: التحقق من النجاح

بعد تشغيل الأوامر:
1. اذهب إلى https://github.com/YOUR-USERNAME/e-store-react
2. يجب أن ترى جميع الملفات موجودة ✅

## 📝 ملفات مهمة موجودة في المشروع:

- `README.md` - توثيق المشروع
- `SETUP_SERVER.md` - شرح تشغيل السيرفر
- `DESIGN_UPDATES.md` - تحسينات التصميم
- `.gitignore` - الملفات المستثناة من Git
- `db.json` - قاعدة البيانات المحلية
- `package.json` - الـ dependencies والـ scripts

## 🔐 كيفية حفظ بيانات الاتصال (اختياري)

إذا أردت تجنب إدخال البيانات كل مرة:

```bash
# احفظ بيانات الدخول
git config --global credential.helper store

# المرة القادمة ستُحفظ البيانات تلقائياً
git push
```

## 📚 أوامر مفيدة للمستقبل:

```bash
# رؤية الـ commits
git log

# رؤية التغييرات غير المرسلة
git status

# إضافة تغييرات جديدة
git add .
git commit -m "رسالة واصفة للتغييرات"
git push

# إنشاء فرع جديد
git checkout -b feature/new-feature

# الدمج بين فرعين
git merge feature/new-feature
```

## ✨ نصائح مهمة:

1. **استخدم رسائل واضحة** في الـ commits
2. **ادفع التغييرات** بانتظام (يومياً أو أسبوعياً)
3. **أنشئ branches** للـ features الجديدة
4. **اكتب README جيد** لشرح المشروع
5. **أضف LICENSE** إذا أردت مشاركة المشروع

---

**هل تحتاج لمساعدة إضافية؟** 😊
