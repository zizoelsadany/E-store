# 📦 كيفية تشغيل المشروع بعد تنزيله من GitHub

## الخطوة 1️⃣: تنزيل المشروع

### من سطر الأوامر:
```bash
git clone https://github.com/YOUR-USERNAME/e-store-react.git
cd e-store-react
```

### أو تنزيل الـ ZIP:
1. اضغط زر **Code** الأخضر
2. اختر **Download ZIP**
3. فك ضغط الملف

## الخطوة 2️⃣: تثبيت الـ Dependencies

```bash
npm install
```

هذا سيثبت جميع المكتبات المطلوبة من `package.json`

## الخطوة 3️⃣: تشغيل السيرفر (JSON Server)

**في نافذة PowerShell أو Terminal جديدة:**

```bash
npm run server
```

سيظهر:
```
JSON Server is running on http://localhost:3001
```

## الخطوة 4️⃣: تشغيل التطبيق (في نافذة جديدة)

```bash
npm run dev
```

سيظهر:
```
VITE v5.0.0  ready in XXX ms

➜  Local:   http://localhost:5173/
```

## الخطوة 5️⃣: فتح المتصفح

اذهب إلى: **http://localhost:5173/**

## 🔐 بيانات تسجيل الدخول التجريبية:

### حساب Admin:
- البريد: `admin@example.com`
- كلمة المرور: `admin123`

### حساب User عادي:
- البريد: `user@example.com`
- كلمة المرور: `user123`

## 📁 هيكل المشروع:

```
e-store-react/
├── src/
│   ├── components/       # المكونات
│   ├── pages/           # الصفحات
│   ├── services/        # خدمات API
│   ├── store/           # حالة التطبيق
│   ├── types/           # أنواع TypeScript
│   ├── hooks/           # Custom Hooks
│   └── App.tsx          # المكون الرئيسي
├── db.json              # قاعدة البيانات
├── package.json         # المكتبات والـ scripts
├── vite.config.ts       # إعدادات Vite
└── tailwind.config.js   # إعدادات TailwindCSS
```

## 🛠️ المكتبات المستخدمة:

- **React 18** - مكتبة الـ UI
- **TypeScript** - للأمان النوعي
- **Vite** - بناء التطبيق بسرعة
- **TailwindCSS** - للـ styling
- **React Router** - للملاحة
- **Zustand** - لإدارة الحالة
- **Axios** - لـ HTTP requests
- **React Hot Toast** - للإشعارات
- **Lucide Icons** - الأيقونات

## 📝 Scripts المتاحة:

```bash
npm run dev       # تشغيل التطبيق في وضع التطوير
npm run build     # بناء النسخة الإنتاجية
npm run preview   # معاينة النسخة الإنتاجية
npm run server    # تشغيل JSON Server
```

## ⚙️ الإعدادات:

### Tailwind CSS:
```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

### لإضافة مكتبة جديدة:
```bash
npm install اسم-المكتبة
```

## 🐛 استكشاف الأخطاء:

### المشكلة: "Cannot find module"
**الحل:**
```bash
npm install
```

### المشكلة: "Port 5173 is already in use"
**الحل:**
```bash
npm run dev -- --port 5174
```

### المشكلة: "Cannot connect to localhost:3001"
**الحل:**
- تأكد من تشغيل السيرفر: `npm run server`
- تأكد من عدم إغلاق نافذة السيرفر

### المشكلة: Database 404 errors
**الحل:**
- تأكد من وجود `db.json` في جذر المشروع
- قم بتشغيل السيرفر: `npm run server`

## 🚀 نصائح للتطوير:

1. **استخدم VS Code**:
   - ثبت extensions: ES7+ React/Redux/React-Native snippets
   - ثبت Tailwind CSS IntelliSense

2. **Hot Reload**: التطبيق يُعدّل تلقائياً عند حفظ الملفات

3. **DevTools**: استخدم F12 في المتصفح لفتح Developer Tools

4. **Git**: اعمل commits بانتظام
   ```bash
   git add .
   git commit -m "رسالة وصفية"
   git push
   ```

## 📚 ملفات توثيق إضافية:

- `README.md` - وثائق المشروع الرئيسية
- `SETUP_SERVER.md` - شرح تفصيلي للسيرفر
- `DESIGN_UPDATES.md` - تحسينات التصميم المطبقة
- `GITHUB_UPLOAD.md` - كيفية رفع المشروع على GitHub

---

**استمتع بالتطوير! 🎉**
